# Evolution Round 7 — AgentMonitorTool Integration

## Timestamp
2026-03-15T18:22:37Z

## Direction
**Type**: INTEGRATION

**Why**: The Historian (Phase-0) reported `NEED_INTEGRATION` — `agent_monitor.py` utility from Round 3 was created but never wired into either entry point. Direction Diversity Rule mandates integration before new features. This round closes that debt.

## Research
Phase-0 research confirmed:
- **Researcher**: Identified observability as key pain point in multi-agent systems
- **Auditor**: Found `agent_monitor.py` exists in `backend/utils/` but is not registered as a tool
- **Historian**: Confirmed `NEED_INTEGRATION` verdict — no evolution-created files were unintegrated except this one

## Changes
**Files Modified/Created**:
1. `backend/tools/agent_monitor_tool.py` (created) — New tool wrapper exposing `get_agent_status_summary()` as an agent-callable tool
2. `tests/test_agent_monitor_tool.py` (created) — 4 tests validating tool interface and behavior
3. `backend/llm/tool_registry.py` (modified) — Registered `AgentMonitorTool` class
4. `main.py` (modified) — Added `watchdog.add_tool(AgentMonitorTool())`
5. `src/tui/agent_bridge.py` (modified) — Added `self._swarm_agent.add_tool(AgentMonitorTool())`
6. `docs/system_design.md` (modified) — Added Round 7 changelog entry and Component Map update

**What it does**: Wraps the existing `agent_monitor.py` utility in a `BaseTool` interface, allowing agents to query swarm status programmatically via `agent_monitor` tool with action `get_status_summary`.

## Test Results
**PASS** — All verification checks passed:
- Import check: `AgentMonitorTool` imports successfully
- Integration check: Wired in BOTH entry points (main.py line 139/430, agent_bridge.py line 37/284)
- Pattern check: Inherits `BaseTool`, has `name`, `description`, `parameters_schema`, `execute()` method
- Duplication check: NONE — no similar tool exists
- Unit tests: 4/4 tests pass

**Reviewer Verdict**: APPROVE — No issues found, proper due diligence, pattern conformance, and integration.

## Verdict
**KEPT** (branch: evolution/r6-20260316_014428, commit 4183c46)

## Integration Actions Taken
- Tool registered in `backend/llm/tool_registry.py`
- Tool added to `main.py` via `watchdog.add_tool()`
- Tool added to `src/tui/agent_bridge.py` via `self._swarm_agent.add_tool()`
- Docs updated with changelog entry and Component Map

## Next Round Suggestion
With `agent_monitor.py` now integrated, agents can programmatically query swarm status. Next improvements could focus on:
1. **Session Replay Tool** — Record agent sessions for post-mortem debugging (Researcher's top candidate)
2. **Memory Drift Detector Middleware** — Catch stale context before it causes failures
3. **TUI Dashboard Enhancement** — Display real-time agent status using the newly-wired tool
4. **Auto-trigger middleware** — Automatically invoke agent monitoring on retry/stall detection
